language: python

python:
    - "2.7"

install:
    - "pip install ."
    - "pip install -r requirements.txt"

env:
    KINETIC_JAR=$(echo $TRAVIS_BUILD_DIR/simulator.jar)

before_script: ./build_simulator.sh

script: nosetests
